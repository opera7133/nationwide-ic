import type { Card, Prefecture } from "../types/cards";

const chibaCards: Card[] = [];

const chiba: Prefecture = {
  id: "chiba",
  code: 12,
  name: "千葉県",
  cards: chibaCards,
};

export { chiba, chibaCards };
