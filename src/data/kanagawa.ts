import type { Card, Prefecture } from "../types/cards";

const kanagawaCards: Card[] = [];

const kanagawa: Prefecture = {
  id: "kanagawa",
  code: 14,
  name: "神奈川県",
  cards: kanagawaCards,
};

export { kanagawa, kanagawaCards };
