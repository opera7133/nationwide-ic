import type { Card, Prefecture } from "../types/cards";

const miyazakiCards: Card[] = [];

const miyazaki: Prefecture = {
  id: "miyazaki",
  code: 45,
  name: "宮崎県",
  cards: miyazakiCards,
};

export { miyazaki, miyazakiCards };
