import type { Card, Prefecture } from "../types/cards";

const ooitaCards: Card[] = [];

const ooita: Prefecture = {
  id: "ooita",
  code: 44,
  name: "大分県",
  cards: ooitaCards,
};

export { ooita, ooitaCards };
