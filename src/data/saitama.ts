import type { Card, Prefecture } from "../types/cards";

const saitamaCards: Card[] = [];

const saitama: Prefecture = {
  id: "saitama",
  code: 11,
  name: "埼玉県",
  cards: saitamaCards,
};

export { saitama, saitamaCards };
