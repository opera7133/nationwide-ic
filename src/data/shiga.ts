import type { Card, Prefecture } from "../types/cards";

const shigaCards: Card[] = [];

const shiga: Prefecture = {
  id: "shiga",
  code: 25,
  name: "滋賀県",
  cards: shigaCards,
};

export { shiga, shigaCards };
