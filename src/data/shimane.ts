import type { Card, Prefecture } from "../types/cards";

const shimaneCards: Card[] = [];

const shimane: Prefecture = {
  id: "shimane",
  code: 32,
  name: "島根県",
  cards: shimaneCards,
};

export { shimane, shimaneCards };
