import type { Card, Prefecture } from "../types/cards";

const tottoriCards: Card[] = [];

const tottori: Prefecture = {
  id: "tottori",
  code: 31,
  name: "鳥取県",
  cards: tottoriCards,
};

export { tottori, tottoriCards };
