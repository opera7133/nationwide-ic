import type { Card, Prefecture } from "../types/cards";

const yamaguchiCards: Card[] = [];

const yamaguchi: Prefecture = {
  id: "yamaguchi",
  code: 35,
  name: "山口県",
  cards: yamaguchiCards,
};

export { yamaguchi, yamaguchiCards };
