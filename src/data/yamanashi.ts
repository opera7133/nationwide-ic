import type { Card, Prefecture } from "../types/cards";

const yamanashiCards: Card[] = [];

const yamanashi: Prefecture = {
  id: "yamanashi",
  code: 19,
  name: "山梨県",
  cards: yamanashiCards,
};

export { yamanashi, yamanashiCards };
